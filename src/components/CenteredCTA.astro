---
const { eyebrow, heading, href, label } = Astro.props;
---

<section class="center-cta" data-reveal>
  <p class="eyebrow">{eyebrow}</p>
  <h2>{heading}</h2>
  <a class="button primary" href={href}>{label}</a>
</section>

<style is:global>
.center-cta { text-align: center; }
</style>
