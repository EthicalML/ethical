---
interface MenuItem {
  number?: string;
  key?: string;
  eyebrow?: string;
  label?: string;
  title?: string;
  description?: string;
  href: string;
}

interface MenuGroup {
  title: string;
  note: string;
  items: MenuItem[];
}

interface MenuColumn {
  heading: string;
  items: MenuItem[];
}

interface Menu {
  label: string;
  width: number;
  primary: boolean;
  eyebrow?: string;
  items?: MenuItem[];
  groups?: MenuGroup[];
  columns?: MenuColumn[];
  footerLinks?: MenuItem[];
}

const menus: Record<string, Menu> = {
  overview: {
    label: 'Overview',
    width: 380,
    primary: true,
    eyebrow: 'ON THIS PAGE',
    items: [
      { number: '01', title: 'Four-phase strategy', href: '/#strategy' },
      { number: '02', title: 'Nine principles', href: '/#principles' },
      { number: '03', title: 'Open source & tools', href: '/#opensource' },
      { number: '04', title: 'Reports & data', href: '/#reports' },
      { number: '05', title: 'Network & newsletter', href: '/#join' },
      { number: '06', title: 'Talks & keynotes', href: '/talks/' },
      { number: '07', title: 'Contact the Institute', href: '/contact/' },
    ],
  },
  policy: {
    label: 'Policy & Frameworks',
    width: 440,
    primary: true,
    items: [
      {
        eyebrow: 'INSTITUTIONAL RECORD',
        title: 'Policy contributions',
        description: 'Public policy work, standards engagements and the published record.',
        href: '/policy/',
      },
      {
        eyebrow: 'FOUNDATION',
        title: 'The nine principles',
        description: 'Commitments, failure modes and the controls that implement them.',
        href: '/principles/',
      },
      {
        eyebrow: 'PROCUREMENT FRAMEWORK',
        title: 'AI-RFX Procurement',
        description: 'Open RFP and RFI templates for machine learning systems.',
        href: '/frameworks/ai-rfx/',
      },
      {
        eyebrow: 'ANNUAL SURVEY',
        title: 'State of Production ML',
        description: 'The annual practitioner survey, compared year on year.',
        href: '/reports/state-of-ml-2025/',
      },
    ],
    footerLinks: [{ label: 'All initiatives', href: '/frameworks/' }],
  },
  oss: {
    label: 'Open source',
    width: 860,
    primary: true,
    items: [
      {
        key: 'kaos',
        eyebrow: 'AGENT INFRASTRUCTURE',
        title: 'K8s Agent OS (KAOS)',
        description: 'Control plane for autonomous agents with scoped credentials, budgets, approval gates and an audit trail.',
        href: '/open-source/kaos/',
      },
      {
        key: 'kompute',
        eyebrow: 'GPU ACCELERATION',
        title: 'Kompute',
        description: 'Cross-vendor GPU compute, portable across hardware, in Linux Foundation AI & Data.',
        href: '/open-source/kompute/',
      },
      {
        key: 'xai',
        eyebrow: 'EXPLAINABILITY',
        title: 'XAI Framework',
        description: 'Data analysis, model evaluation and production monitoring for bias and explainability.',
        href: '/open-source/xai/',
      },
      {
        key: 'list',
        eyebrow: 'ECOSYSTEM',
        title: 'Production ML list',
        description: 'Community catalogue of production ML and MLOps tooling, curated weekly.',
        href: '/open-source/production-ml-list/',
      },
    ],
    footerLinks: [{ label: 'All projects', href: '/open-source/' }],
  },
  initiatives: {
    label: 'Initiatives',
    width: 880,
    primary: true,
    eyebrow: 'PRINCIPLES, FRAMEWORKS & DATA',
    groups: [
      {
        title: 'Principles & frameworks',
        note: 'APPLIED FRAMEWORKS',
        items: [
          { label: 'The nine principles', description: 'Commitments, failure modes, controls', href: '/principles/' },
          { label: 'Frameworks overview', description: 'Procurement, maturity and security', href: '/frameworks/' },
          { label: 'AI-RFX Procurement', description: 'Open RFP and RFI templates', href: '/frameworks/ai-rfx/' },
          { label: 'ML Maturity Model', description: 'Eight supplier assessment criteria', href: '/frameworks/maturity-model/' },
          { label: 'MLSecOps & agentic security', description: 'Lifecycle and agentic security taxonomy', href: '/frameworks/security/' },
          { label: 'Agentic RFX', description: 'Procurement criteria in development', href: '/frameworks/agentic-rfx/' },
          { label: 'Agentic Maturity Model', description: 'Operating capability in development', href: '/frameworks/agentic-maturity-model/' },
        ],
      },
      {
        title: 'Reports & data',
        note: 'PUBLISHED EVIDENCE',
        items: [
          { label: 'State of Production ML 2025', description: 'Practitioner survey, tools and statistics', href: '/reports/state-of-ml-2025/' },
          { label: '2024 edition', description: 'Previous annual survey', href: '/reports/state-of-ml-2024/' },
          { label: 'Methodology & citation', description: 'Fieldwork, samples and citation guidance', href: '/reports/' },
        ],
      },
      {
        title: 'Institutional work',
        note: 'INSTITUTIONAL WORK',
        items: [
          { label: 'Policy & standards', description: 'Public policy, standards and institutional records', href: '/policy/' },
          { label: 'Partners', description: 'Institutional relationships and roles', href: '/partners/' },
          { label: 'Open-source work', description: 'Practical tools and shared infrastructure', href: '/open-source/' },
          { label: 'Contact the Institute', description: 'Start an institutional collaboration', href: '/contact/' },
        ],
      },
    ],
  },
  network: {
    label: 'Network',
    width: 640,
    primary: true,
    columns: [
      {
        heading: 'JOIN',
        items: [
          { title: 'The ML Engineer newsletter', description: 'Weekly issues for 70k+ practitioners, with the full archive', href: '/newsletter/' },
          { title: 'Ethical AI Network', description: 'Members across engineering, research, policy and standards', href: '/network/' },
          { title: 'Membership', description: 'Contribution, benefits and how to apply', href: '/membership/' },
        ],
      },
      {
        heading: 'CONNECT',
        items: [
          { title: 'Talks & events', description: 'Keynotes, conference talks and roundtables', href: '/talks/' },
          { title: 'Partners', description: 'Institutional collaboration', href: '/partners/' },
          { title: 'Policy & standards', description: 'Engagements and public records', href: '/policy/' },
        ],
      },
    ],
  },
  about: {
    label: 'About',
    width: 340,
    primary: false,
    eyebrow: 'THE INSTITUTE',
    items: [
      { number: '01', title: 'Our story', href: '/about/' },
      { number: '02', title: 'Partners', href: '/partners/' },
      { number: '03', title: 'Contact', href: '/contact/' },
    ],
  },
};
const wordmark = {
  primary: 'THE INSTITUTE FOR ETHICAL AI',
  secondary: 'ALIGNMENT + SAFETY',
};
const join = {
  label: 'JOIN / CONTACT',
  href: '/contact/?interest=newsletter#contact',
};
const initialProject = menus.oss.items![0]!;
import CommandPalette from './CommandPalette.astro';
---

<site-header transition:persist="site-header">
  <header class="site-header">
    <div class="header-row">
      <a class="wordmark" href="/">
        <span>{wordmark.primary}</span>
        <span>{wordmark.secondary}</span>
      </a>
      <nav class="primary-nav" aria-label="Primary navigation">
        {
          Object.entries(menus)
            .filter(([, menu]) => menu.primary)
            .map(([id, menu]) => (
              <button class="nav-trigger" type="button" aria-expanded="false" data-menu-trigger={id} data-menu-width={menu.width}>
                {menu.label}
                <span class="caret" aria-hidden="true">
                  ▾
                </span>
              </button>
            ))
        }
      </nav>
      <div class="nav-right">
        <button class="search-trigger" type="button" data-command-palette-open aria-label="Search the site">
          <span aria-hidden="true">⌘K</span>
        </button>
        <button class="nav-trigger" type="button" aria-expanded="false" data-menu-trigger="about" data-menu-width={menus.about.width}>
          {menus.about.label}<span class="caret" aria-hidden="true">▾</span>
        </button>
        <a class="join-pill" href={join.href}>{join.label}</a>
        <button class="mobile-menu-toggle" type="button" aria-expanded="false" aria-controls="mobile-menu" data-mobile-menu-open>
          <span>Menu</span><i aria-hidden="true"></i>
        </button>
      </div>
    </div>
    <div class="menu-wrap">
      <div class="mega-panel" data-menu-panel aria-hidden="true">
        {
          Object.entries(menus).map(([id, menu]) => (
            <div class:list={['mega-panel-body', id === 'overview' && 'is-active']} data-menu-body={id}>
              {menu.eyebrow && !menu.groups && <p class="menu-eyebrow">{menu.eyebrow}</p>}
              {menu.columns ? (
                <div class="network-menu">
                  {menu.columns.map(({ heading, items }) => (
                    <div>
                      <p>{heading}</p>
                      <div>
                        {items.map(({ title, description, href }) => (
                          <a href={href}>
                            <strong>{title}</strong>
                            <span>{description}</span>
                          </a>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              ) : menu.groups ? (
                <div class="initiative-menu">
                  <div class="group-rail">
                    {menu.groups.map(({ title, note }, index) => (
                      <button type="button" class:list={{ active: index === 0 }} data-initiative-index={index}>
                        <strong>{title}</strong>
                        <span>{note}</span>
                        <i />
                      </button>
                    ))}
                  </div>
                  <div class="initiative-panes">
                    {menu.groups.map(({ note, items }, index) => (
                      <div class:list={['initiative-pane', index === 0 && 'active']} data-initiative-pane={index}>
                        <p>{note}</p>
                        <div>
                          {items.map(({ label, description, href }) => (
                            <a href={href}>
                              <strong>{label}</strong>
                              <span>{description}</span>
                            </a>
                          ))}
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              ) : id === 'policy' ? (
                <div class="oss-menu-list standalone">
                  {menu.items!.map(({ eyebrow, title, description, href }) => (
                    <a href={href}>
                      <span>{eyebrow}</span>
                      <strong>{title}</strong>
                      <small>{description}</small>
                    </a>
                  ))}
                  <footer>
                    {menu.footerLinks!.map(({ label, href }) => (
                      <a class="button primary" href={href}>
                        {label}
                      </a>
                    ))}
                  </footer>
                </div>
              ) : id === 'oss' ? (
                <div class="oss-menu">
                  <div class="oss-menu-list">
                    {menu.items!.map(({ key, eyebrow, title, description, href }, index) => (
                      <a
                        href={href}
                        class:list={{ active: index === 0 }}
                        data-oss-index={index}
                        data-oss-key={key}
                        data-oss-eyebrow={eyebrow}
                        data-oss-title={title}
                        data-oss-description={description}
                      >
                        <span>{eyebrow}</span>
                        <strong>{title}</strong>
                        <small>{description}</small>
                      </a>
                    ))}
                    <footer>
                      {menu.footerLinks!.map(({ label, href }) => (
                        <a class="button primary" href={href}>
                          {label}
                        </a>
                      ))}
                    </footer>
                  </div>
                  <div class="oss-menu-preview">
                    <nav-preview mode={initialProject.key}>
                      <canvas aria-hidden="true" />
                    </nav-preview>
                    <p>
                      <span data-oss-preview-eyebrow>{initialProject.eyebrow}</span>
                      <strong data-oss-preview-title>{initialProject.title}</strong>
                      <i data-oss-preview-description>{initialProject.description}</i>
                    </p>
                  </div>
                </div>
              ) : (
                <div class:list={['menu-link-list', id === 'network' && 'two-columns']}>
                  {menu.items!.map((item, index) => (
                    <a href={item.href} style={`--row-index:${index}`}>
                      {(id === 'overview' || id === 'about') && <span>{item.number}</span>}
                      <strong>{item.title}</strong>
                      <span aria-hidden="true" />
                    </a>
                  ))}
                </div>
              )}
            </div>
          ))
        }
      </div>
    </div>
    <div class="mobile-drawer" id="mobile-menu" role="dialog" aria-modal="true" aria-label="Site navigation" aria-hidden="true" data-mobile-menu>
      <div class="mobile-drawer-header">
        <a class="wordmark" href="/">
          <span>{wordmark.primary}</span>
          <span>{wordmark.secondary}</span>
        </a>
        <a class="join-pill" href={join.href}>{join.label}</a>
        <button class="mobile-menu-close" type="button" aria-label="Close menu" data-mobile-menu-close>
          <span aria-hidden="true"></span>
        </button>
      </div>
      <nav class="mobile-nav" aria-label="Mobile navigation">
        {
          Object.entries(menus).map(([id, menu], menuIndex) => (
            <section class="mobile-menu-section" style={`--menu-index:${menuIndex}`}>
              <button type="button" aria-expanded="false" aria-controls={`mobile-submenu-${id}`} data-mobile-accordion>
                <span>{String(menuIndex + 1).padStart(2, '0')}</span>
                <strong>{menu.label}</strong>
                <i aria-hidden="true">+</i>
              </button>
              <div class="mobile-submenu" id={`mobile-submenu-${id}`} hidden>
                {menu.columns ? (
                  menu.columns.map((column) => (
                    <div class="mobile-link-group">
                      <p>{column.heading}</p>
                      {column.items.map((item) => (
                        <a href={item.href}>
                          <strong>{item.title}</strong>
                          <span>{item.description}</span>
                        </a>
                      ))}
                    </div>
                  ))
                ) : menu.groups ? (
                  menu.groups.map((group) => (
                    <div class="mobile-link-group">
                      <p>{group.title}</p>
                      {group.items.map((item) => (
                        <a href={item.href}>
                          <strong>{item.label}</strong>
                          <span>{item.description}</span>
                        </a>
                      ))}
                    </div>
                  ))
                ) : (
                  <div class="mobile-link-group">
                    {menu.items!.map((item) => (
                      <a href={item.href}>
                        <strong>{item.title}</strong>
                        {item.description && <span>{item.description}</span>}
                      </a>
                    ))}
                    {menu.footerLinks?.map((item) => (
                      <a href={item.href}>
                        <strong>{item.label}</strong>
                      </a>
                    ))}
                  </div>
                )}
              </div>
            </section>
          ))
        }
      </nav>
    </div>
  </header>
  <CommandPalette />
</site-header>

<style>
  .oss-menu-list.standalone {
    border-right: 0;
  }
  .search-trigger {
    background: none;
    border: 1px solid var(--hairline);
    border-radius: 4px;
    color: var(--text-2);
    cursor: pointer;
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    padding: 5px 8px;
    transition:
      border-color 0.2s ease,
      color 0.2s ease;
  }
  .search-trigger:hover {
    border-color: rgba(94, 230, 160, 0.45);
    color: var(--accent);
  }
  @media (max-width: 950px) {
    .search-trigger {
      display: none;
    }
  }
</style>

<script>
  import '../shared/canvas/NavPreview';

  class SiteHeaderElement extends HTMLElement {
    private closeTimer?: number;
    private controller = new AbortController();

    connectedCallback() {
      this.controller = new AbortController();
      this.setupMenu();
      this.setupMobileMenu();
    }

    disconnectedCallback() {
      this.controller.abort();
      if (this.closeTimer !== undefined) window.clearTimeout(this.closeTimer);
    }

    private setupMenu() {
      const header = this.querySelector<HTMLElement>('.site-header');
      const panel = header?.querySelector<HTMLElement>('[data-menu-panel]');
      const triggers = Array.from(header?.querySelectorAll<HTMLElement>('[data-menu-trigger]') ?? []);
      const bodies = Array.from(header?.querySelectorAll<HTMLElement>('[data-menu-body]') ?? []);
      const headerRow = header?.querySelector<HTMLElement>('.header-row');
      if (!header || !panel || !headerRow || !triggers.length) return;

      let active: string | null = null;
      const signal = this.controller.signal;

      const measure = (trigger: HTMLElement) => {
        const rowBounds = headerRow.getBoundingClientRect();
        const triggerBounds = trigger.getBoundingClientRect();
        const width = Math.min(Number(trigger.dataset.menuWidth), rowBounds.width - 80);
        const left = Math.max(40, Math.min(triggerBounds.left - rowBounds.left, rowBounds.width - width - 40));
        panel.style.setProperty('--panel-left', `${left}px`);
        panel.style.setProperty('--panel-width', `${width}px`);
      };
      const close = () => {
        if (this.closeTimer !== undefined) window.clearTimeout(this.closeTimer);
        active = null;
        panel.classList.remove('is-open');
        panel.setAttribute('aria-hidden', 'true');
        triggers.forEach((trigger) => trigger.setAttribute('aria-expanded', 'false'));
      };
      const open = (trigger: HTMLElement) => {
        if (this.closeTimer !== undefined) window.clearTimeout(this.closeTimer);
        const id = trigger.dataset.menuTrigger;
        if (!id || id === active) return;
        measure(trigger);
        triggers.forEach((item) => {
          item.setAttribute('aria-expanded', String(item === trigger));
        });
        bodies.forEach((body) => {
          const selected = body.dataset.menuBody === id;
          body.classList.toggle('is-active', selected);
          if (selected) {
            body.style.animation = 'none';
            requestAnimationFrame(() => {
              body.style.animation = 'om-swap .3s cubic-bezier(.16,1,.3,1)';
            });
          }
        });
        panel.setAttribute('aria-hidden', 'false');
        panel.classList.add('is-open');
        active = id;
      };
      const delayedClose = () => {
        if (this.closeTimer !== undefined) window.clearTimeout(this.closeTimer);
        this.closeTimer = window.setTimeout(close, 240);
      };

      triggers.forEach((trigger) => {
        trigger.addEventListener('mouseenter', () => open(trigger), { signal });
        trigger.addEventListener('focus', () => open(trigger), { signal });
        trigger.addEventListener(
          'click',
          () => {
            if (active === trigger.dataset.menuTrigger) close();
            else open(trigger);
          },
          { signal },
        );
      });
      header.addEventListener(
        'mouseenter',
        () => {
          if (this.closeTimer !== undefined) window.clearTimeout(this.closeTimer);
        },
        { signal },
      );
      header.addEventListener('mouseleave', delayedClose, { signal });
      panel.querySelectorAll('a').forEach((link) => {
        link.addEventListener('click', close, { signal });
      });

      const previewCanvas = panel.querySelector<HTMLElement>('nav-preview');
      const previewEyebrow = panel.querySelector<HTMLElement>('[data-oss-preview-eyebrow]');
      const previewTitle = panel.querySelector<HTMLElement>('[data-oss-preview-title]');
      const previewDescription = panel.querySelector<HTMLElement>('[data-oss-preview-description]');
      panel.querySelectorAll<HTMLElement>('[data-oss-index]').forEach((row) => {
        const select = () => {
          if (row.classList.contains('active')) return;
          panel.querySelectorAll('[data-oss-index]').forEach((item) => {
            item.classList.toggle('active', item === row);
          });
          if (row.dataset.ossKey) previewCanvas?.setAttribute('mode', row.dataset.ossKey);
          if (previewEyebrow) previewEyebrow.textContent = row.dataset.ossEyebrow ?? '';
          if (previewTitle) previewTitle.textContent = row.dataset.ossTitle ?? '';
          if (previewDescription) {
            previewDescription.textContent = row.dataset.ossDescription ?? '';
          }
          const preview = panel.querySelector<HTMLElement>('.oss-menu-preview');
          if (preview) {
            preview.style.animation = 'none';
            requestAnimationFrame(() => {
              preview.style.animation = 'om-swap .3s cubic-bezier(.16,1,.3,1)';
            });
          }
        };
        row.addEventListener('mouseenter', select, { signal });
        row.addEventListener('focus', select, { signal });
      });

      panel.querySelectorAll<HTMLElement>('[data-initiative-index]').forEach((row) => {
        const select = () => {
          const index = row.dataset.initiativeIndex;
          panel.querySelectorAll('[data-initiative-index]').forEach((item) => {
            item.classList.toggle('active', item === row);
          });
          panel.querySelectorAll<HTMLElement>('[data-initiative-pane]').forEach((pane) => {
            pane.classList.toggle('active', pane.dataset.initiativePane === index);
          });
        };
        row.addEventListener('mouseenter', select, { signal });
        row.addEventListener('focus', select, { signal });
      });

      document.addEventListener(
        'click',
        (event) => {
          if (!header.contains(event.target as Node)) close();
        },
        { signal },
      );
      document.addEventListener(
        'keydown',
        (event) => {
          if (event.key === 'Escape') close();
        },
        { signal },
      );
      window.addEventListener(
        'resize',
        () => {
          const trigger = triggers.find((item) => item.dataset.menuTrigger === active);
          if (trigger && active) measure(trigger);
        },
        { signal },
      );
      window.addEventListener(
        'scroll',
        () => {
          if (window.scrollY > header.offsetHeight) close();
        },
        { passive: true, signal },
      );
    }

    private setupMobileMenu() {
      const drawer = this.querySelector<HTMLElement>('[data-mobile-menu]');
      const openButton = this.querySelector<HTMLButtonElement>('[data-mobile-menu-open]');
      const closeButton = drawer?.querySelector<HTMLButtonElement>('[data-mobile-menu-close]');
      if (!drawer || !openButton || !closeButton) return;

      const accordions = Array.from(drawer.querySelectorAll<HTMLButtonElement>('[data-mobile-accordion]'));
      const signal = this.controller.signal;
      let lastFocus: HTMLElement | null = null;
      let lockedScroll = 0;
      const focusable = () =>
        Array.from(drawer.querySelectorAll<HTMLElement>('a[href], button:not([disabled])')).filter((element) => !element.closest('[hidden]'));
      const close = () => {
        if (drawer.getAttribute('aria-hidden') === 'true') return;
        drawer.setAttribute('aria-hidden', 'true');
        openButton.setAttribute('aria-expanded', 'false');
        document.body.classList.remove('mobile-nav-open');
        document.body.style.top = '';
        scrollTo(0, lockedScroll);
        lastFocus?.focus();
      };
      const open = () => {
        lastFocus = document.activeElement instanceof HTMLElement ? document.activeElement : null;
        lockedScroll = scrollY;
        document.body.style.top = `-${lockedScroll}px`;
        document.body.classList.add('mobile-nav-open');
        drawer.setAttribute('aria-hidden', 'false');
        openButton.setAttribute('aria-expanded', 'true');
        closeButton.focus();
      };

      openButton.addEventListener('click', open, { signal });
      closeButton.addEventListener('click', close, { signal });
      drawer.querySelectorAll('a').forEach((link) => {
        link.addEventListener('click', close, { signal });
      });
      accordions.forEach((button) => {
        button.addEventListener(
          'click',
          () => {
            const expanded = button.getAttribute('aria-expanded') === 'true';
            const controls = button.getAttribute('aria-controls');
            const submenu = controls ? drawer.querySelector<HTMLElement>(`#${controls}`) : null;
            button.setAttribute('aria-expanded', String(!expanded));
            if (submenu) submenu.hidden = expanded;
          },
          { signal },
        );
      });
      document.addEventListener(
        'keydown',
        (event) => {
          if (drawer.getAttribute('aria-hidden') === 'true') return;
          if (event.key === 'Escape') {
            event.preventDefault();
            close();
            return;
          }
          if (event.key !== 'Tab') return;
          const items = focusable();
          const first = items[0];
          const last = items[items.length - 1];
          if (!first || !last) return;
          if (event.shiftKey && document.activeElement === first) {
            event.preventDefault();
            last.focus();
          } else if (!event.shiftKey && document.activeElement === last) {
            event.preventDefault();
            first.focus();
          }
        },
        { signal },
      );
      matchMedia('(min-width: 951px)').addEventListener(
        'change',
        (event) => {
          if (event.matches) close();
        },
        { signal },
      );
    }
  }

  if (!customElements.get('site-header')) {
    customElements.define('site-header', SiteHeaderElement);
  }
</script>

<style is:global>
.site-header {
  backdrop-filter: blur(14px);
  background: var(--bg-header);
  border-bottom: 1px solid var(--hairline);
  position: sticky;
  top: 0;
  /* Pin the header to its own compositor layer: without this, Chrome re-rasterizes the
     backdrop-filter whenever a canvas beneath promotes/demotes layers, which flickers. */
  transform: translateZ(0);
  will-change: backdrop-filter;
  z-index: 60;
}

.header-row {
  align-items: center;
  display: flex;
  gap: var(--space-26);
  padding: 18px 40px;
}

.wordmark {
  display: flex;
  flex: none;
  flex-direction: column;
  font-family: var(--font-mono);
  font-size: var(--mono-10);
  letter-spacing: .15em;
  line-height: 1.08;
  white-space: nowrap;
}

.wordmark span:last-child { color: var(--accent); }

.primary-nav {
  align-items: center;
  display: flex;
  flex: none;
}

.nav-trigger {
  background: transparent;
  border: 0;
  border-radius: var(--radius-pill);
  color: var(--text-2);
  cursor: pointer;
  font-size: var(--body-13);
  padding: 8px 9px;
}

.nav-trigger[aria-expanded="true"] {
  background: rgba(255, 255, 255, .07);
  color: var(--text-1);
}

.caret {
  font-size: 8px;
  transition: transform var(--duration-3) var(--ease-panel);
}

[aria-expanded="true"] .caret { transform: rotate(180deg); }

.join-pill {
  border: 1px solid rgba(94, 230, 160, .45);
  border-radius: var(--radius-control);
  color: var(--accent);
  font-family: var(--font-mono);
  font-size: var(--mono-10-5);
  letter-spacing: .1em;
  margin-left: auto;
  padding: 9px 13px;
  white-space: nowrap;
}

.menu-wrap {
  height: 0;
  left: 0;
  position: absolute;
  right: 0;
  top: 100%;
}

.menu-eyebrow {
  color: var(--text-4);
  font-family: var(--font-mono);
  font-size: var(--mono-9);
  letter-spacing: .16em;
  margin: 0;
  padding: 0 12px 12px;
}

@keyframes menu-row {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: none; }
}

.site-header { view-transition-name: site-header; }

@media (max-width: 950px) {
  .header-row {
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 14px;
      padding: 16px 24px;
    }
}

@media (max-width: 950px) {
  .wordmark { width: 100%; }
}

@media (max-width: 950px) {
  .primary-nav { order: 3; }
}

@media (max-width: 950px) {
  .join-pill {
      margin-left: 0;
      position: absolute;
      right: 24px;
      top: 18px;
    }
}

.nav-right { align-items: center; display: flex; gap: 8px; margin-left: auto; }

.nav-right .join-pill { margin-left: 0; }

.mega-panel {
  --panel-left: 40px;
  --panel-width: 380px;
  background: var(--bg-panel);
  border: 1px solid var(--hairline-card);
  box-shadow: var(--shadow-panel);
  filter: blur(3px);
  left: var(--panel-left);
  opacity: 0;
  overflow: hidden;
  pointer-events: none;
  position: absolute;
  top: 0;
  transform: translateY(-8px);
  transform-origin: top left;
  transition: left .42s var(--ease-panel), width .42s var(--ease-panel), opacity .3s var(--ease-panel), transform .3s var(--ease-panel), filter .3s;
  width: var(--panel-width);
}

.mega-panel.is-open { filter: none; opacity: 1; pointer-events: auto; transform: none; }

.mega-panel-body { display: none; min-height: 280px; padding: 24px 26px; }

.mega-panel-body.is-active { display: block; }

.mega-panel-body[data-menu-body="oss"],
.mega-panel-body[data-menu-body="policy"],
.mega-panel-body[data-menu-body="network"] { min-height: 0; padding: 0; }

.menu-link-list { display: flex; flex-direction: column; }

.menu-link-list.two-columns { display: grid; grid-template-columns: 1fr 1fr; gap: 1px 20px; }

.menu-link-list a {
  align-items: center;
  border-top: 1px solid var(--hairline);
  display: grid;
  font-size: var(--body-14-5);
  grid-template-columns: 30px 1fr auto;
  min-height: 41px;
  padding: 0 12px;
}

.menu-link-list a:hover, .menu-card-grid a:hover { background: var(--accent-wash-07); opacity: 1; }

.menu-link-list a span, .menu-card-grid span {
  color: var(--text-4); font-family: var(--font-mono); font-size: var(--mono-9); font-weight: 400;
}

.menu-link-list a strong { font-weight: 400; }

.oss-menu { display: grid; grid-template-columns: 320px minmax(0,1fr); margin: 0; }

.oss-menu-list { border-right: 1px solid rgba(255,255,255,.08); display: flex; flex-direction: column; gap: 1px; padding: 18px 20px; }

.oss-menu-list > a { display: flex; flex-direction: column; min-width: 0; padding: 10px 14px; }

.oss-menu-list a.active, .oss-menu-list a:hover { background: rgba(94,230,160,.08); opacity: 1; }

.oss-menu-list span { color: rgba(244,242,238,.4); font: 8.5px/1 var(--font-mono); letter-spacing: .16em; }

.oss-menu-list > a.active span { color: var(--accent); }

.oss-menu-list strong { font-size: 15.5px; font-weight: 400; margin-top: 4px; }

.oss-menu-list small { color: rgba(244,242,238,.55); font-size: 12.5px; line-height: 1.45; margin-top: 2px; }

.oss-menu-list footer { align-items: flex-start; border-top: 1px solid rgba(255,255,255,.08); display: flex; flex-direction: column; font: 9.5px/1.4 var(--font-mono); gap: 8px; letter-spacing: .14em; margin-top: 12px; padding: 12px 12px 0; }

.oss-menu-preview { background: var(--bg-inset); min-height: 300px; overflow: hidden; position: relative; }

.oss-menu-preview canvas {
  height: 100%;
  inset: 0;
  mask-image: linear-gradient(180deg, #000 42%, transparent 94%);
  -webkit-mask-image: linear-gradient(180deg, #000 42%, transparent 94%);
  position: absolute;
  width: 100%;
}

.oss-menu-preview p { bottom: 22px; display: flex; flex-direction: column; gap: 4px; left: 26px; margin: 0; position: absolute; right: 26px; }

.oss-menu-preview p span { color: var(--accent); font: 8.5px/1 var(--font-mono); letter-spacing: .16em; }

.oss-menu-preview p strong { font: 300 26px/1.2 var(--font-display); }

.oss-menu-preview p i { color: rgba(244,242,238,.6); font-size: 13px; font-style: normal; line-height: 1.5; max-width: 420px; }

.network-menu { display: grid; gap: 34px; grid-template-columns: repeat(2,minmax(0,1fr)); height: 314.5px; padding: 28px 34px; }

.network-menu > div > p { color: rgba(244,242,238,.35); font: 9px/1 var(--font-mono); letter-spacing: .16em; margin: 0; }

.network-menu > div > div { display: flex; flex-direction: column; gap: 2px; margin-top: 12px; }

.network-menu a { border-radius: 0; display: flex; flex-direction: column; gap: 2px; padding: 9px 12px; }

.network-menu a:hover { background: rgba(255,255,255,.05); opacity: 1; }

.network-menu strong { font-size: 14.5px; font-weight: 400; }

.network-menu span { color: rgba(244,242,238,.55); font-size: 12.5px; line-height: 1.45; }

.mega-panel-body[data-menu-body="overview"] { height: 365px; }

.mega-panel-body[data-menu-body="initiatives"] { height: 340px; padding: 0; }

.mega-panel-body[data-menu-body="about"] { height: 323px; }

.initiative-menu { display: grid; grid-template-columns: 230px 1fr; height: 340px; margin: 0; }

.group-rail { border-right: 1px solid var(--hairline); display:flex; flex-direction:column; gap:2px; padding:24px 18px; }

.group-rail button { align-items:center; background: none; border: 0; border-radius:0; color: inherit; column-gap:10px; display: grid; grid-template-columns: 1fr auto; padding: 11px 13px; row-gap:2px; text-align: left; width: 100%; }

.group-rail button.active { background: var(--accent-wash-09); }

.group-rail strong { font-size: 14.5px; font-weight: 400; }

.group-rail span { color: var(--text-4); font: 8.5px/1.4 var(--font-mono); grid-column: 1; letter-spacing:.14em; }

.group-rail i { color: var(--accent); grid-column: 2; grid-row: 1 / span 2; }

.initiative-panes { padding:26px 30px; }

.initiative-pane { display:none; }

.initiative-pane.active { display:block; }

.initiative-pane > p { color:var(--text-4); font:9px var(--font-mono); letter-spacing:.16em; margin:0; }

.initiative-pane > div { display:grid; gap:4px 26px; grid-template-columns:1fr 1fr; margin-top:14px; }

.initiative-pane a { border-radius:0; display:flex; flex-direction:column; gap:2px; min-width:0; padding:10px 12px; }

.initiative-pane a:hover { background:rgba(255,255,255,.05); }

.initiative-pane strong { font-size:14.5px; font-weight:400; }

.initiative-pane span { color:rgba(244,242,238,.55); font-size:12.5px; line-height:1.45; }

@keyframes om-swap { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: none; } }

@media (max-width: 950px) {
  .primary-nav { display: none; }
}

@media (max-width: 950px) {
  .nav-right .nav-trigger { display: none; }
}

@media (max-width: 950px) {
  .mega-panel { display: none; }
}

@media (max-width: 950px) {
  .nav-right { margin-left: auto; }
}

@media (max-width: 950px) {
  .nav-right .join-pill { position: static; }
}

.mobile-menu-toggle,
.mobile-drawer { display: none; }
</style>

<style is:global>
@media (max-width: 950px) {
  .header-row {
      align-items: center;
      flex-wrap: nowrap;
      gap: 10px;
      min-height: 68px;
      padding: 12px 20px;
    }
}

@media (max-width: 950px) {
  .header-row > .wordmark {
      font-size: 9px;
      justify-content: center;
      min-height: 44px;
      min-width: 0;
      width: auto;
    }
}

@media (max-width: 950px) {
  .nav-right { gap: 8px; }
}

@media (max-width: 950px) {
  .nav-right .join-pill {
      align-items: center;
      display: inline-flex;
      min-height: 44px;
      padding: 9px 14px;
      position: static;
    }
}

@media (max-width: 950px) {
  .mobile-menu-toggle {
      align-items: center;
      background: transparent;
      border: 1px solid var(--hairline-card);
      border-radius: var(--radius-pill);
      color: var(--text-1);
      cursor: pointer;
      display: inline-flex;
      font: var(--mono-9)/1 var(--font-mono);
      gap: 9px;
      justify-content: center;
      letter-spacing: .1em;
      min-height: 44px;
      padding: 0 13px;
      text-transform: uppercase;
    }
}

@media (max-width: 950px) {
  .mobile-menu-toggle i,
    .mobile-menu-toggle i::before,
    .mobile-menu-toggle i::after {
      background: currentColor;
      display: block;
      height: 1px;
      width: 16px;
    }
}

@media (max-width: 950px) {
  .mobile-menu-toggle i { position: relative; }
}

@media (max-width: 950px) {
  .mobile-menu-toggle i::before,
    .mobile-menu-toggle i::after {
      content: "";
      left: 0;
      position: absolute;
    }
}

@media (max-width: 950px) {
  .mobile-menu-toggle i::before { top: -5px; }
}

@media (max-width: 950px) {
  .mobile-menu-toggle i::after { top: 5px; }
}

@media (max-width: 950px) {
  .mobile-drawer {
      background: var(--bg-panel);
      display: block;
      height: 100dvh;
      inset: 0;
      min-height: 100vh;
      opacity: 0;
      overflow-y: auto;
      overscroll-behavior: contain;
      pointer-events: none;
      position: fixed;
      transform: translateY(-12px);
      transition: opacity var(--duration-3) var(--ease-panel), transform var(--duration-3) var(--ease-panel), visibility var(--duration-3);
      visibility: hidden;
      z-index: 80;
    }
}

@media (max-width: 950px) {
  .mobile-drawer[aria-hidden="false"] {
      opacity: 1;
      pointer-events: auto;
      transform: none;
      visibility: visible;
    }
}

@media (max-width: 950px) {
  .mobile-drawer-header {
      align-items: center;
      background: var(--bg-header);
      border-bottom: 1px solid var(--hairline);
      display: flex;
      gap: 10px;
      min-height: 68px;
      padding: 12px 20px;
      position: sticky;
      top: 0;
      z-index: 2;
    }
}

@media (max-width: 950px) {
  .mobile-drawer-header .wordmark {
      font-size: 9px;
      justify-content: center;
      margin-right: auto;
      min-height: 44px;
      width: auto;
    }
}

@media (max-width: 950px) {
  .mobile-drawer-header .join-pill {
      align-items: center;
      display: inline-flex;
      margin-left: 0;
      min-height: 44px;
      padding: 9px 14px;
    }
}

@media (max-width: 950px) {
  .mobile-menu-close {
      background: transparent;
      border: 1px solid var(--hairline-card);
      border-radius: 50%;
      color: var(--text-1);
      cursor: pointer;
      height: 44px;
      position: relative;
      width: 44px;
    }
}

@media (max-width: 950px) {
  .mobile-menu-close span::before,
    .mobile-menu-close span::after {
      background: currentColor;
      content: "";
      height: 1px;
      left: 13px;
      position: absolute;
      top: 21px;
      width: 16px;
    }
}

@media (max-width: 950px) {
  .mobile-menu-close span::before { transform: rotate(45deg); }
}

@media (max-width: 950px) {
  .mobile-menu-close span::after { transform: rotate(-45deg); }
}

@media (max-width: 950px) {
  .mobile-nav {
      margin: 0 auto;
      max-width: 760px;
      padding: 24px 20px 48px;
    }
}

@media (max-width: 950px) {
  .mobile-menu-section {
      border-top: 1px solid var(--hairline);
      padding: 0;
    }
}

@media (max-width: 950px) {
  .mobile-menu-section:last-child { border-bottom: 1px solid var(--hairline); }
}

@media (max-width: 950px) {
  .mobile-menu-section > button {
      align-items: center;
      background: transparent;
      border: 0;
      color: var(--text-1);
      cursor: pointer;
      display: grid;
      grid-template-columns: 38px 1fr 28px;
      min-height: 60px;
      padding: 8px 4px;
      text-align: left;
      width: 100%;
    }
}

@media (max-width: 950px) {
  .mobile-menu-section > button span {
      color: var(--text-4);
      font: var(--mono-9)/1 var(--font-mono);
      letter-spacing: .12em;
    }
}

@media (max-width: 950px) {
  .mobile-menu-section > button strong {
      font: 300 25px/1.1 var(--font-display);
    }
}

@media (max-width: 950px) {
  .mobile-menu-section > button i {
      color: var(--accent);
      font: normal 22px/1 var(--font-body);
      text-align: center;
      transition: transform var(--duration-3) var(--ease-panel);
    }
}

@media (max-width: 950px) {
  .mobile-menu-section > button[aria-expanded="true"] i { transform: rotate(45deg); }
}

@media (max-width: 950px) {
  .mobile-submenu { padding: 0 0 18px 38px; }
}

@media (max-width: 950px) {
  .mobile-link-group + .mobile-link-group { margin-top: 18px; }
}

@media (max-width: 950px) {
  .mobile-link-group > p {
      color: var(--accent);
      font: var(--mono-9)/1 var(--font-mono);
      letter-spacing: .14em;
      margin: 8px 0 6px;
      text-transform: uppercase;
    }
}

@media (max-width: 950px) {
  .mobile-link-group a {
      display: flex;
      flex-direction: column;
      justify-content: center;
      min-height: 48px;
      padding: 7px 8px;
    }
}

@media (max-width: 950px) {
  .mobile-link-group a strong { font-size: 14px; font-weight: 400; }
}

@media (max-width: 950px) {
  .mobile-link-group a span {
      color: var(--text-3);
      font-size: 12px;
      line-height: 1.4;
      margin-top: 2px;
    }
}

@media (max-width: 950px) {
  .mobile-drawer[aria-hidden="false"] .mobile-menu-section {
      animation: menu-row var(--duration-34) var(--ease-panel) both;
      animation-delay: calc(var(--menu-index) * 35ms);
    }
}

@media (max-width: 420px) {
  .header-row,
    .mobile-drawer-header { padding-inline: 14px; }
}

@media (max-width: 420px) {
  .nav-right .join-pill,
    .mobile-drawer-header .join-pill { padding-inline: 11px; }
}

@media (max-width: 420px) {
  .mobile-menu-toggle {
      font-size: 0;
      padding-inline: 13px;
    }
}

@media (max-width: 420px) {
  .mobile-nav { padding-inline: 14px; }
}
</style>
