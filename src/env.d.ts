/* Replaced at build by the Vite define in astro.config.mjs: the form receiver's
   URL, base64-encoded, inlined into the client bundle instead of the markup. */
declare const __FORM_TOKEN__: string;
